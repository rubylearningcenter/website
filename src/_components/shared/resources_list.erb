<% resource_groups.each do |group_name, resources| %>
  <h2><%= group_name %></h2>

  <ul>
    <% resources.each do |resource| %>
      <li>
        <a href="<%= resource.relative_url %>"><%= resource.data.title %></a>
      </li>
    <% end %>
  </ul>
<% end %>
